$schema: "http://json-schema.org/draft-07/schema#"

description: an entry in the sample sheet
properties:
    sample-id:
        type: string
        description: sample name
    r1:
        type: string
        description: path to the forward reads
    r2:
        type: string
        description: path to the reverse reads (if paried end)

required:
    - sample
    - r1
